{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            // "command": "${workspaceFolder}/build/qemu-system-arm -M musca-a -nographic -S -d cpu,int,guest_errors,unimp,in_asm -monitor /dev/pts/0 2>&1 -device loader,file=../prebuilt_image/ota_boot1.bin,addr=0xC000000 | tee monitor.log",
            "command": "make -j10",
            "options": {
                "cwd": "${workspaceFolder}/build/"
            }
            
        }
    ]
}
